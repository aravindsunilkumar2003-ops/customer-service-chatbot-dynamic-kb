# customer-service-chatbot-dynamic-kb
**Dynamic KB Chatbot** – Extends a LangChain + FAISS customer service bot with automatic knowledge base updates. Ingests CSV, JSON, URLs &amp; text files, deduplicates via SHA-256, and incrementally merges new embeddings into FAISS. Includes APScheduler for periodic updates and a Streamlit management UI.
